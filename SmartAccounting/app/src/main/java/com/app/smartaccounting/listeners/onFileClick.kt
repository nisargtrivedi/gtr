package com.app.smartaccounting.listeners

import com.app.smartaccounting.model.ReceivedMessage
import com.app.smartaccounting.model.categoryModel

interface onFileClick {
    fun onClick(model: ReceivedMessage)

}